{
  "name": "@acme/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:phase1": "playwright test --project=setup --project=phase-1-guest --project=phase-1-authenticated --project=phase-1-api",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@types/node": "^25.5.0",
    "typescript": "^5.7.0"
  }
}
